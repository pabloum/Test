import logo from '../logo.svg';

const Header = (props) => {
    return <>
                <h1>THE USERS APP</h1>
                <img src={logo} className="App-logo" alt="logo" />
           </>
}

export default Header